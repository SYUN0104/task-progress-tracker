<script lang="ts">
  // Minimal scaffold shell — replaced by the real Active/Hold/Archive
  // sections and domain-backed store in later phases (see plan Phase 3).
  let appName = 'Task Progress Tracker';
</script>

<main>
  <h1>{appName}</h1>
  <p>Scaffold shell — UI implementation lands in later phases.</p>
</main>

<style>
  main {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    color: var(--color-text, #e6e6e6);
    background: var(--color-bg, #1e1e1e);
  }
</style>
