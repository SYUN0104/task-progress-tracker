[package]
name = "persist"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"
description = "Pure-Rust state persistence logic (no Tauri deps) - unit-testable in WSL"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
